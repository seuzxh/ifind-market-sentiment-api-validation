# 终端 Agent 验收记录

验收日期：2026-09-09 11:47（Asia/Shanghai）
代码版本：`bc03311`
规则版本：`rules-v1`

## 验收用例

| 用例 | 结果 | 说明 |
|---|---|---|
| `status --date 2026-09-08 --json --no-save` | 通过 | HTTP 200、业务 `errorcode=0`；实际日期为 2026-09-08，分段 B |
| `forecast --date 2026-09-08 --json --no-save` | 通过 | 输出规则型下一交易日倾向，不输出概率 |
| `status --date 2026-05-25 --json --no-save` | 通过 | 退出码 1；明确标记边界日，不发起替代日期查询 |
| 无 `IFIND_ACCESS_TOKEN` 执行 `status` | 通过 | 退出码 2；在发起请求前失败，不回显令牌 |
| `python -m unittest discover -s tests -v` | 通过 | 27 项测试通过 |
| `python -m py_compile market_agent.py` | 通过 | 编译检查通过 |

## 2026-09-08 结果摘要

真实接口返回的日频结果为：主基准收益 `0.00666194`、Ret5 `0.00207686`、Ret20 `0.02493045`、Ret60 `-0.01542138`；SIZE/RISK/MOM Spread 分别为 `0.01100631`、`-0.00792691`、`-0.01020204`；上涨比例 `0.62550413`、净广度 `0.26848473`。规则输出方向“偏强”、风险模式“Risk-Off”。

广度来自 `data_pool/p00112` 的 `p0=A股` 候选参数，结果质量为“有限可用”，范围标签为 `A_candidate_SH_SZ`。这表示接口调用和字段对齐成功，不表示供应商已经确认沪深京纯 A 语义。

## 数据准入与限制

- `history_data` 使用用户确认的 `CPS=1` 后复权；指数前后复权数值相同不改变请求参数记录。`vwap` 记录为均价，首版不用于方向判断。
- `p00112` 的上涨、平盘、下跌映射为 `f002/f003/f004`；停牌和纯 A 沪深京完整口径仍需供应商字段说明。缺失值保留为 `None`。
- `real_time_quotation` 已有 2026-09-09 盘中成功证据；空的 `suspensionCount` 不当作零。高频接口已确认当前日可用，历史分钟回放仍未验证。
- 2026-05-25 的异常跳变只作为质量事件记录。所有窗口、趋势、Spread 和回溯均在 A 段（截至 2026-05-22）或 B 段（自 2026-05-26）内计算。
- 本次令牌只从启动进程的 `IFIND_ACCESS_TOKEN` 读取；证据和结果中没有认证头或令牌原文。`data/` 已忽略，不发布到 GitHub Pages。

## 后续待确认

1. 供应商确认 `p00112` 是否覆盖沪深京纯 A，以及平盘、停牌和新股口径。
2. 供应商补充 `history_data` 的量额、换手率、VWAP 单位和修订机制；CPS 语义按新端点单独维护。
3. 如需历史分钟回放、多人访问或高频重复检索，再分别评估设计中的方案二（SQLite）和方案三（Web/API）。
