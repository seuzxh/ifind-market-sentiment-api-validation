# REST 行情质量核对

18条正式指数的多日返回包含18个代码，覆盖2026-04-01至2026-09-08。每个代码的`time`与指标数组等长，日期按升序返回；`chg`和`pct_chg`与收盘/昨收的算术关系在容差内成立。

CPS缺省、CPS=1、CPS=2在2026-09-08主基准上的结果逐字段相同。这只能说明该日期/指数区间数值相同，不能确认附件所述CPS=1后复权，也不能确认参数被忽略。

2026-09-05周末返回空`tables`且`errorcode=0`，应归一化为“非交易日无行情”，不能补前一日数据。2026-09-10未来日期返回一行：`pre_close`与`close`有值，其余行情字段为空；应标记为未来占位，不能当作真实行情。

发现严重异常：昨日连板`883958.TI`、昨日首板表现`883979.TI`、昨日涨停表现`883900.TI`在2026-05-25附近出现约-100%的涨跌幅和数量级跳变。虽然字段恒等式仍成立，但这可能是基准、编制或数据修订变化；在供应商解释前隔离该日期段，不能用于收益、Spread或模型训练。

成交量、成交额、VWAP和换手率已返回，但单位及VWAP计算对象尚未确认。尤其主基准`vwap=16.7188`与`close=1911.648`数量级不同，不能把VWAP直接作为指数点位均价。
